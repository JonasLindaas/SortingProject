package IO;

import java.util.ArrayList;

/**
 * Handles input :-] I'll elaborate later, the first handler will be fairly simple
 */
public interface IInputHandler {
    /**
     * Reads input from somewhere and returns it as an ArrayList of Strings
     * @return input in the form of an arraylist of strings
     */
    ArrayList<String> readInput();
}
